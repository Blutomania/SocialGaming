# Choose Your Mystery - Database System Requirements

# Core dependencies
anthropic>=0.18.0           # Claude API for AI-powered extraction and generation
requests>=2.31.0            # HTTP requests for web scraping
beautifulsoup4>=4.12.0      # HTML parsing for Project Gutenberg
python-dotenv>=1.0.0        # Environment variable management

# Corpus loading (mystery-crime-books Parquet dataset)
pyarrow>=14.0.0             # Read .parquet files
pandas>=2.1.0               # DataFrame manipulation for corpus rows

# NLP and text processing
spacy>=3.7.0                # Natural language processing (optional for future use)
# Run after install: python -m spacy download en_core_web_sm

# Database (for future PostgreSQL migration)
psycopg2-binary>=2.9.9      # PostgreSQL adapter (optional for now)

# Optional: Vector search (for production)
# pgvector>=0.2.0           # PostgreSQL vector extension
# sentence-transformers>=2.2.0  # Embeddings generation

# Optional: Async processing (for scaling)
# aiohttp>=3.9.0            # Async HTTP requests
# asyncio                   # Built-in, for concurrent processing

# Development tools
# pytest>=7.4.0             # Testing framework
# black>=23.0.0             # Code formatting
# flake8>=6.1.0             # Linting
